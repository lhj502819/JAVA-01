spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/order_db?useUnicode=true&characterEncoding=utf-8&rewriteBatchedStatements=true
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    hikari:
      readOnly: false
      connectionTimeout: 30000
      idleTimeout: 60000
      validationTimeout: 3000
      maxLifetime: 120000
      loginTimeout: 5
      maximumPoolSize: 100
      minimumIdle: 10
      connection-test-query: select 1
